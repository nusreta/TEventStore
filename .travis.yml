language: shell
os: windows
dist: trusty

before_script:
- choco install mssqlserver2014-sqllocaldb
- powershell -Command "Set-ExecutionPolicy Bypass"

script:
  - dotnet restore
  - dotnet build
  - dotnet test
